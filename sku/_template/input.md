# SKU input template

Edit `input.csv` or pass another CSV to the runner.

- Keep one SKU and one `core_keyword` per file.
- Use `start_year` and `end_year` for continuous fitment.
- Use semicolon-separated `years` for non-contiguous fitment.
- Add vehicle-family mappings in `config/vehicle_family.csv`, not in product input.
