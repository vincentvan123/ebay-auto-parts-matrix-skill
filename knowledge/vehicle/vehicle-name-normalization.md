# Vehicle name normalization

Canonical names preserve market-facing casing. Add verified make/model mappings only when source capitalization differs from the compatibility catalog.

Normalization is keyed by case-insensitive make/model pairs. Unknown names are retained after whitespace cleanup and surfaced for review.
